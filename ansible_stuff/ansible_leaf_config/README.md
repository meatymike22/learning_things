# Configure NXOS leaf switch BGP

run the following command to execute the playbook:

For entire playbook:
* ansible-playbook nxos_fabric.yml

For bgp tasks:
* ansible-playbook nxos_fabric.yml --tags "bgp"

Ansible [Documentation](https://docs.ansible.com) 
